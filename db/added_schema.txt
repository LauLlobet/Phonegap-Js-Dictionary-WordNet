CREATE TABLE `selected_senses` (
  `ssenseid`integer NOT NULL DEFAULT '0'
,  `ctime` integer DEFAULT NULL
,  `dtime` integer DEFAULT NULL
,  `wordid` integer  NOT NULL DEFAULT '0'
,  `synsetid` integer  DEFAULT NULL
,  PRIMARY KEY (`ssenseid`)
);


CREATE VIEW selected_words AS SELECT lemma, ctime,dtime,wordid,synsetid FROM selected_senses LEFT JOIN words s USING (wordid) ORDER BY ctime DESC GROUP BY lemma;


-----------------------------------------
senseid -> ssenseid i afegirlo a la view selected_words


-----------------------------------------




CREATE TABLE `favourites_senses` (
  `senseid`integer NOT NULL DEFAULT '0'
,  `ctime` integer DEFAULT NULL
,  `dtime` integer DEFAULT NULL
,  `wordid` integer  NOT NULL DEFAULT '0'
,  `synsetid` integer  DEFAULT NULL
,  PRIMARY KEY (`senseid`)
);